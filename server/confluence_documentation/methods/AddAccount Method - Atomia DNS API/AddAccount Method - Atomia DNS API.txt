{toc:maxLevel=3|type=flat|separator=pipe|includePages=true}

Adds an account with a specified username and password.

h3.Declaration syntax
{panel}
void AddAccount(
	string email,
	string password
)
{panel}

h3.Parameters

|| Parameter || Type || Description ||
|email|string|the username of the account (email)|
|password|string|the password of the account|

{include:AddAccount Method Example - Atomia DNS API}
